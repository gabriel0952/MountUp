import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('MountUp smoke test', (WidgetTester tester) async {
    // Placeholder - real tests will be added per feature
    expect(1 + 1, 2);
  });
}
